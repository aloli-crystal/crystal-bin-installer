= Changelog
:toc: left
:toc-title: Versions
:toclevels: 2

== v0.1.3 — 2026-04-25

Symlink install mode. New `-l` / `--link` CLI flag (and matching
`link: true` config key) installs each binary as a symlink to
`bin/<target>` inside the source project, instead of copying the
file. Any subsequent `shards build` inside a project is then
immediately reflected in `~/bin` without re-running the installer
— very convenient when iterating on a tool you also use daily.

* Symlink target is always an absolute path, so the link survives
  a relative `--dest` or a later move of `~/bin`.
* The destination is wiped and re-created on every run, so a
  switch between `--link` and the default copy mode is idempotent
  (no leftover broken symlinks, no stale copies).
* `--no-link` is provided as a counterpart to override
  `link: true` from the config file.
* The success line of each install reads `lié : <path>` instead
  of `installé : <path>` when the symlink mode is active, to make
  the choice explicit in the run log.
* 4 new unit tests covering: config key parsing (`link: true`),
  symlink creation pointing at the source, copy mode preserved
  when `link: false`, and idempotent replacement of an existing
  destination. **11 unit tests, all green.**

== v0.1.2 — 2026-04-22

* Add a persistent per-user config file, loaded by default from
  `~/.crystal-bin-installer.yml`. Supported keys: `dir`, `dest`,
  `release`, `fetch`, `force`, `skip` (list). The path can be
  overridden with `--config PATH`.
* Precedence is now: built-in defaults < config file < CLI flags.
  `--skip` is additive with the config file (union of both lists);
  all other options override.
* The CLI help banner documents the config file location and the
  list of supported keys.

== v0.1.1 — 2026-04-22

* When a project declares exactly one target, the installed binary now
  takes the project (directory) name instead of the target name. This
  avoids generic target names leaking into `~/bin` and shadowing other
  tools on `$PATH` (e.g. `crystal-deploy` used to install as `deploy`).
* Projects with multiple targets keep the previous behaviour: each
  binary is installed under its own target name.

== v0.1.0 — 2026-04-22

First public release.

* Walks a source directory (default `~/prod-crystal`) and detects every
  Crystal project declaring a `targets:` section in its `shard.yml`.
* Checks git synchronisation against `origin/production`: current branch,
  clean working tree, remote in sync.
* Runs `shards install --production` and `shards build` (with `--release`
  unless `--dev` is passed).
* Copies the produced binary to the destination directory (default `~/bin`).
* CLI options: `--dir`, `--dest`, `--dev`, `--skip`, `--dry-run`,
  `--no-fetch`, `--force`, `-v`, `-h`.
* Prints a coloured summary at the end (installed / failed / skipped).
